module.exports = {
    name: 'verify',
    permissions: [""],
    description: 'Sets up a reation role message',

    async execute(message, args, cmd, client, discord, profileData) {
        
    }
}